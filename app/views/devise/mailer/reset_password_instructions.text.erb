Hello <%= @resource.first_name %>,

A password reset was requested for your <%= @site_name %> account.

Choose a new password:
<%= edit_password_url(@resource, reset_password_token: @token) %>

If you did not request this, you can ignore the message. Your password will not change unless the link is used.
